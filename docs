<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Quantara Canon Kernel</title>
  <style>
    body {
      font-family: "Inter", sans-serif;
      background-color: #0a0a0a;
      color: #f1f1f1;
      text-align: center;
      margin: 0;
      padding: 2rem;
    }
    h1 {
      color: #00ffc6;
      margin-bottom: 0.5rem;
    }
    p {
      color: #bbb;
      max-width: 600px;
      margin: 0 auto 1.5rem;
      line-height: 1.6;
    }
    button {
      background: linear-gradient(135deg, #00ffc6, #0078ff);
      border: none;
      border-radius: 8px;
      color: #000;
      font-weight: bold;
      font-size: 1rem;
      padding: 0.8rem 1.2rem;
      cursor: pointer;
      transition: 0.2s;
    }
    button:hover {
      filter: brightness(1.2);
      transform: translateY(-1px);
    }
    footer {
      margin-top: 3rem;
      color: #666;
      font-size: 0.9rem;
    }
  </style>
</head>
<body>
  <h1>Quantara Canon Kernel</h1>
  <p>
    Stateless alignment kernel implementing π→φ→e reasoning with κ/τ/Σ ethics,  
    zero-return, cycle detection, and fact-guard grounding.<br><br>
    Press the button below to copy the latest kernel code to your clipboard.
  </p>

  <button onclick="copyCanon()">📋 Copy Canon Kernel</button>
  <p id="status" style="margin-top:1rem;color:#00ffc6;"></p>

  <script>
    async function copyCanon() {
      const url = "https://raw.githubusercontent.com/quantumquantara-arch/quantara-canon/main/quantara_canon_boot.py";
      try {
        const response = await fetch(url);
        const code = await response.text();
        await navigator.clipboard.writeText(code);
        const status = document.getElementById("status");
        status.textContent = "✓ Canon Kernel copied to clipboard!";
        setTimeout(() => status.textContent = "", 3000);
      } catch (err) {
        alert("⚠️ Could not copy kernel. Check your connection or repository privacy settings.");
      }
    }
  </script>

  <footer>© Quantara Canon • Stateless Alignment Architecture • ○ → ∞</footer>
</body>
</html>
